<!-- TAMPLATE FOR CHECK -->
<div class="form-check">
    <label class="form-check-label">
        <input name="{{ $name }}" value="{{ $name }}" type="checkbox" class="form-check-input" checked>
        {{ $value }}
    </label>
</div>